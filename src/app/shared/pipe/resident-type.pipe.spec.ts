import { ResidentTypePipe } from './resident-type.pipe';

describe('ResidentTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ResidentTypePipe();
    expect(pipe).toBeTruthy();
  });
});
